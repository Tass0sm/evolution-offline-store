Installation Instructions
*************************

To configure the project and build the project do the following:

	mkdir build && cd build
	meson ..
	ninja
	sudo ninja install
	
To see details run meson configure
